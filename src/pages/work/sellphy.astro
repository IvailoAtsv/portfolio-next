---
import BaseLayout from '../../layouts/BaseLayout.astro';
import StudioNav from '../../components/StudioNav.astro';
import ArrowIcon from '../../components/ArrowIcon.astro';
import { pages } from '../../lib/site';

const assets = '/assets/projects/sellphy';
---

<BaseLayout page={pages.sellphy}>
  <div class="site-shell case-shell sellphy-case">
    <StudioNav />
    <main id="main-content">
      <article class="case-study">
        <header class="case-hero sellphy-hero" data-motion-group>
          <div class="case-heading">
            <a class="pill-link back-link" href="/#work">
              <ArrowIcon direction="left" /> Back to the reel
            </a>
            <h1 class="hero-title" data-motion="title">
              Sellphy<span class="title-outline">.</span>
            </h1>
            <p data-motion="copy">
              Sellphy is a shop platform I designed and built. A seller enters
              their company details, adds their products, walks through six
              steps, and has a working shop with checkout, legal pages and a
              domain. There is no agency in the middle and nothing to install.
              Shops go live about 70% faster than on comparable platforms, and
              serving them from a small VPS cut my hosting bill by 83%.
            </p>
            <div class="case-actions" data-motion="action">
              <a class="button button-dark" href="#builder" data-squash>
                How a shop gets built <ArrowIcon direction="down" />
              </a>
            </div>
          </div>

          <figure class="sellphy-stage" data-motion="cel" data-motion-order="2">
            <a
              class="screen-window"
              href={`${assets}/builder.webp`}
              target="_blank"
              rel="noreferrer"
              aria-label="Open the Sellphy site builder screenshot at full size"
            >
              <div class="screen-bar" aria-hidden="true">
                <span></span><span></span><span></span><b
                  >Site editor / step 2 of 6</b
                >
              </div>
              <img
                src={`${assets}/builder.webp`}
                alt="Sellphy site editor on step 2 of 6, with a live preview of a video homepage hero and layout variants in the right panel"
                width="1024"
                height="661"
                fetchpriority="high"
              />
            </a>
            <figcaption>
              Step 2 of 6 in the site editor: the homepage. The preview in the
              middle is the live shop, so there is nothing to publish and check
              afterwards.<br /><span>Demo shop. Select to enlarge.</span>
            </figcaption>
          </figure>

          <dl class="case-meta">
            <div data-motion="detail">
              <dt>Role</dt><dd>Product design<br />Full-stack development</dd>
            </div>
            <div data-motion="detail">
              <dt>Product</dt><dd>
                Shop platform<br />Storefronts · Dashboard · AI flows
              </dd>
            </div>
            <div data-motion="detail">
              <dt>System</dt><dd>
                Next.js · TypeScript<br />MongoDB · Docker on Hetzner
              </dd>
            </div>
            <div data-motion="detail">
              <dt>Outcome</dt><dd>
                Shops live 70% faster<br />25+ hours saved a month
              </dd>
            </div>
          </dl>
        </header>

        <div class="wave-divider case-wave" data-loop aria-hidden="true">
          <svg viewBox="0 0 2880 96" preserveAspectRatio="none"
            ><path
              d="M0 51c80-42 160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0s160 42 240 0v80H0Z"
            ></path></svg
          >
        </div>

        <div class="case-body">
          <nav class="case-nav" aria-label="Case study sections">
            <span>On this reel</span>
            <a href="#builder">The editor</a>
            <a href="#product-pages">Product pages</a>
            <a href="#dashboard">The dashboard</a>
            <a href="#ai">AI flows</a>
            <a href="#tours">Tours</a>
            <a href="#ux">Finding things</a>
            <a href="#static">Hosting</a>
          </nav>

          <div class="case-content">
            <dl
              class="sellphy-stats"
              aria-label="What Sellphy does for a seller"
              data-motion-group
            >
              <div data-motion="detail">
                <dt>70%</dt>
                <dd>
                  faster to a live shop. Six guided steps instead of a theme
                  marketplace and a plugin hunt.
                </dd>
              </div>
              <div data-motion="detail">
                <dt>90%</dt>
                <dd>
                  less time on legal boilerplate. Privacy, terms and returns
                  pages are generated for each shop.
                </dd>
              </div>
              <div data-motion="detail">
                <dt>75%</dt>
                <dd>
                  higher on Google. AI-drafted custom pages, checked in Page
                  Preview before they go out.
                </dd>
              </div>
              <div data-motion="detail">
                <dt>83%</dt>
                <dd>
                  lower hosting cost. Public pages are built once and served as
                  files from a small VPS.
                </dd>
              </div>
            </dl>

            <section
              class="case-section case-opening"
              id="builder"
              data-motion-group
            >
              <h2 data-motion="title">A shop in six steps.</h2>
              <div class="case-columns">
                <p class="case-lede" data-motion="copy">
                  Company details and products go in first. Then the site editor
                  walks the seller through layout, homepage, shop page,
                  navigation, colors and fonts, and that is the whole setup.
                  There is no theme to buy and no plugin to find, which is why a
                  shop on Sellphy goes live about 70% faster than on comparable
                  platforms.
                </p>
                <div>
                  <p data-motion="copy">
                    Most of the work is in the homepage step: each section has a
                    handful of variants, including a video hero. The preview in
                    the middle is rendered with the same components as the live
                    shop, so the page a seller approves is the page the customer
                    gets. You can click a heading in the preview and type
                    straight into it instead of finding the matching field in
                    the side panel. A product grid in the editor is the grid the
                    customer will see.
                  </p>
                  <p data-motion="copy">
                    When the six steps are done the shop is live on a
                    sellphy.store subdomain, with its privacy policy, terms and
                    returns pages already in place. Those are generated per shop
                    and follow the shop's details, so nobody is pasting
                    boilerplate from a template site. I put the time that saves
                    on legal pages at about 90%. Connecting a custom domain is a
                    dashboard setting.
                  </p>
                </div>
              </div>
              <ol
                class="setup-sequence"
                aria-label="Site editor steps"
                data-motion="detail"
              >
                <li>
                  <strong>Layout</strong><span>Width, corners, spacing</span>
                </li>
                <li>
                  <strong>Home</strong><span>Sections and their variants</span>
                </li>
                <li>
                  <strong>Shop</strong><span>The catalog page</span>
                </li>
                <li>
                  <strong>Navigation</strong><span>Menu and footer</span>
                </li>
                <li>
                  <strong>Colors</strong><span>A preset or your own</span>
                </li>
                <li>
                  <strong>Fonts</strong><span>Heading and body pair</span>
                </li>
              </ol>
            </section>

            <section class="case-section" id="product-pages" data-motion-group>
              <h2 data-motion="title">
                One product page,<br />three shops.
              </h2>
              <div class="case-columns">
                <p class="case-lede" data-motion="copy">
                  Every shop on Sellphy uses the same product page. What differs
                  is settings: theme, gallery, trust cards, whether the quantity
                  field shows, and what the button says. Variants can be size
                  pills, color swatches or a dropdown, depending on what the
                  shop sells.
                </p>
                <div>
                  <p data-motion="copy">
                    DEMO sells clothes, so the variants are sizes and colors and
                    the button says add to request. Alcona sells fence panels,
                    so the variant is a length dropdown and the button says add
                    to calculator. Gymleco sells gym equipment on a dark theme
                    with feature cards and a specs table.
                  </p>
                  <p data-motion="copy">
                    I wrote the page once. Each shop dresses it from the
                    dashboard, so a seller changes how the page sells without
                    touching code, and a fix I make lands in every shop the same
                    day.
                  </p>
                </div>
              </div>
            </section>

            <div class="sellphy-product-strip">
              <figure data-motion-group>
                <a
                  class="screen-window"
                  href={`${assets}/product-demo.webp`}
                  target="_blank"
                  rel="noreferrer"
                  aria-label="Open the DEMO product page screenshot at full size"
                  data-motion="cel"
                >
                  <div class="screen-bar" aria-hidden="true">
                    <span></span><span></span><span></span><b>DEMO / apparel</b>
                  </div>
                  <img
                    src={`${assets}/product-demo.webp`}
                    alt="DEMO shop product page for a white linen shirt with color and size choices, add to request, trust cards, and a specifications table"
                    width="1024"
                    height="710"
                    loading="lazy"
                  />
                </a>
                <figcaption>
                  <strong>DEMO</strong><span
                    >Sizes, colors, add to request. The demo shop; select to
                    enlarge.</span
                  >
                </figcaption>
              </figure>
              <figure data-motion-group>
                <a
                  class="screen-window"
                  href={`${assets}/product-alcona.webp`}
                  target="_blank"
                  rel="noreferrer"
                  aria-label="Open the Alcona product page screenshot at full size"
                  data-motion="cel"
                >
                  <div class="screen-bar" aria-hidden="true">
                    <span></span><span></span><span></span><b>Alcona / fences</b
                    >
                  </div>
                  <img
                    src={`${assets}/product-alcona.webp`}
                    alt="Alcona Solutions product page for a WPC fence panel with color swatches, a length dropdown, and an add to calculator button"
                    width="1024"
                    height="690"
                    loading="lazy"
                  />
                </a>
                <figcaption>
                  <strong>Alcona</strong><span
                    >Color swatches, a length dropdown, add to calculator.
                    Select to enlarge.</span
                  >
                </figcaption>
              </figure>
              <figure data-motion-group>
                <a
                  class="screen-window"
                  href={`${assets}/product-gymleco.webp`}
                  target="_blank"
                  rel="noreferrer"
                  aria-label="Open the Gymleco product page screenshot at full size"
                  data-motion="cel"
                >
                  <div class="screen-bar" aria-hidden="true">
                    <span></span><span></span><span></span><b
                      >Gymleco / equipment</b
                    >
                  </div>
                  <img
                    src={`${assets}/product-gymleco.webp`}
                    alt="Gymleco dark product page for an ISO lateral bench press with quantity, add to request, feature cards, and specifications"
                    width="1024"
                    height="681"
                    loading="lazy"
                  />
                </a>
                <figcaption>
                  <strong>Gymleco</strong><span
                    >Dark theme, feature cards, a specs table. Select to
                    enlarge.</span
                  >
                </figcaption>
              </figure>
            </div>

            <section class="case-section" id="dashboard" data-motion-group>
              <h2 data-motion="title">Where sellers spend the day.</h2>
              <div class="case-columns">
                <p class="case-lede" data-motion="copy">
                  Products, orders, inventory, promo codes, analytics, custom
                  pages, the blog, the site editor, SEO tools and domains all
                  live in one dashboard. It is in Bulgarian, because that is the
                  language the sellers work in, and it assumes no training,
                  because most sellers arrive with none.
                </p>
                <div>
                  <p data-motion="copy">
                    The products screen is a table: thumbnail, status, price,
                    discount, variant count, sizes and categories, with filters
                    across the top. Ticking rows opens bulk edit, so repricing
                    forty shirts before a sale is one form, not forty.
                  </p>
                  <p data-motion="copy">
                    Page Preview takes any URL from the shop and shows what
                    Google, Facebook and X will display for it, with character
                    counts on the title and description. A title under 30
                    characters gets a warning. The audit table underneath lists
                    every field it checked. Sellers fix the snippet before a
                    page goes out instead of finding out weeks later in Search
                    Console.
                  </p>
                </div>
              </div>
            </section>

            <div class="case-proof-pair sellphy-stacked-pair">
              <figure data-motion-group>
                <a
                  class="screen-window"
                  href={`${assets}/products.webp`}
                  target="_blank"
                  rel="noreferrer"
                  aria-label="Open the Sellphy products screenshot at full size"
                  data-motion="cel"
                >
                  <div class="screen-bar" aria-hidden="true">
                    <span></span><span></span><span></span><b>Products</b>
                  </div>
                  <img
                    src={`${assets}/products.webp`}
                    alt="Sellphy products table with thumbnails, published status, euro prices, discounts, variant counts, and size pills"
                    width="1024"
                    height="658"
                    loading="lazy"
                  />
                </a>
                <figcaption>
                  <strong>The products table</strong><span
                    >Filters across the top, bulk edit through the checkboxes.
                    Demo shop; select to enlarge.</span
                  >
                </figcaption>
              </figure>
              <figure data-motion-group>
                <a
                  class="screen-window"
                  href={`${assets}/seo-preview.webp`}
                  target="_blank"
                  rel="noreferrer"
                  aria-label="Open the Sellphy page preview screenshot at full size"
                  data-motion="cel"
                >
                  <div class="screen-bar" aria-hidden="true">
                    <span></span><span></span><span></span><b>Page preview</b>
                  </div>
                  <img
                    src={`${assets}/seo-preview.webp`}
                    alt="Sellphy page preview tool showing a Google search snippet, Open Graph cards, and an audit table flagging a short title and description"
                    width="1024"
                    height="660"
                    loading="lazy"
                  />
                </a>
                <figcaption>
                  <strong>Page Preview</strong><span
                    >The Google snippet, the Open Graph cards and the audit
                    table for one category page. Demo shop; select to enlarge.</span
                  >
                </figcaption>
              </figure>
            </div>

            <figure class="case-screen" data-motion-group>
              <a
                class="screen-window"
                href={`${assets}/analytics.webp`}
                target="_blank"
                rel="noreferrer"
                aria-label="Open the Sellphy analytics dashboard screenshot at full size"
                data-motion="cel"
              >
                <div class="screen-bar" aria-hidden="true">
                  <span></span><span></span><span></span><b
                    >Analytics / demo data</b
                  >
                </div>
                <img
                  src={`${assets}/analytics.webp`}
                  alt="Sellphy analytics dashboard with page view, visitor, cart, and order totals above a visits chart and a top pages table"
                  width="1024"
                  height="659"
                  loading="lazy"
                />
              </a>
              <figcaption>
                <strong>Analytics without a cookie banner</strong><span
                  >Page views, unique visitors, add-to-carts and orders, then
                  the visits chart and the top pages. Visitors are counted
                  without cookies. The figures are demo data.</span
                >
              </figcaption>
            </figure>

            <section class="case-section" id="ai" data-motion-group>
              <h2 data-motion="title">
                The AI drafts,<br />the seller decides.
              </h2>
              <div class="case-columns">
                <p class="case-lede" data-motion="copy">
                  A seller who wants a Father's Day shirt promotion types that
                  one sentence into the custom pages screen, picks Flash or Pro,
                  ticks the shirts it should feature, chooses a mood, and gets a
                  finished landing page with its title, URL and SEO metadata
                  already filled in. Building that page by hand used to be an
                  afternoon.
                </p>
                <div>
                  <p data-motion="copy">
                    The catalog basis is what keeps the page honest. Anchored to
                    eight selected products or a category, the AI writes about
                    things the shop sells, at the prices in the database. The
                    mood presets, editorial, typographic, narrative or catalog,
                    pick the layout family, so the result lands inside the
                    shop's design system rather than beside it.
                  </p>
                  <p data-motion="copy">
                    The blog assistant works the same way. The seller gives a
                    topic and a notes-and-facts field for what the AI must not
                    invent, and it proposes a plan, drafts the post and runs an
                    editorial check. The seller decides what stays. Slug,
                    indexing and the featured image sit in the publishing panel
                    on the right, and Page Preview checks the result before it
                    goes out. Custom pages built this way rank about 75% higher
                    on Google than the pages sellers were putting up before, and
                    a week of content fits in an evening.
                  </p>
                  <p data-motion="copy">
                    The field for what the AI must not invent is the design
                    decision I argued for hardest. A shop owner knows the return
                    window and the fabric weight; the model does not, and it
                    will fill the gap with something plausible. Making the
                    seller write the facts down first is a small chore that
                    removes confidently wrong copy before it is written.
                  </p>
                </div>
              </div>
            </section>

            <div class="case-proof-pair sellphy-stacked-pair">
              <figure data-motion-group>
                <a
                  class="screen-window"
                  href={`${assets}/ai-page-generator.webp`}
                  target="_blank"
                  rel="noreferrer"
                  aria-label="Open the Sellphy AI page generator screenshot at full size"
                  data-motion="cel"
                >
                  <div class="screen-bar" aria-hidden="true">
                    <span></span><span></span><span></span><b
                      >Custom pages / generate with AI</b
                    >
                  </div>
                  <img
                    src={`${assets}/ai-page-generator.webp`}
                    alt="Sellphy generate with AI dialog with Flash selected over Pro, a one-sentence brief for a Father's Day shirt promotion, eight of twelve shirt products ticked, and four mood presets"
                    width="1024"
                    height="660"
                    loading="lazy"
                  />
                </a>
                <figcaption>
                  <strong>One sentence in</strong><span
                    >Flash or Pro, the brief, eight of twelve shirts as the
                    catalog basis, and a mood. Title, URL and SEO are filled in
                    automatically. Demo shop; select to enlarge.</span
                  >
                </figcaption>
              </figure>
              <figure data-motion-group>
                <a
                  class="screen-window"
                  href={`${assets}/ai-blog-assistant.webp`}
                  target="_blank"
                  rel="noreferrer"
                  aria-label="Open the Sellphy blog post assistant screenshot at full size"
                  data-motion="cel"
                >
                  <div class="screen-bar" aria-hidden="true">
                    <span></span><span></span><span></span><b>Blog / new post</b
                    >
                  </div>
                  <img
                    src={`${assets}/ai-blog-assistant.webp`}
                    alt="Sellphy new blog post screen with the AI assistant panel for topic and notes, a propose a plan button, the post form below, and publishing settings on the right"
                    width="1024"
                    height="659"
                    loading="lazy"
                  />
                </a>
                <figcaption>
                  <strong>The post assistant</strong><span
                    >Topic, then the notes and facts the AI must not invent,
                    then a proposed plan. Status, slug, indexing and the
                    featured image live in the right column. Demo shop; select
                    to enlarge.</span
                  >
                </figcaption>
              </figure>
            </div>

            <section class="case-section" id="tours" data-motion-group>
              <h2 data-motion="title">Every screen explains itself.</h2>
              <div class="case-columns">
                <p class="case-lede" data-motion="copy">
                  Each main dashboard screen has a guided tour. It starts on the
                  first visit, and the question mark in the header replays it.
                  Completion is stored per shop. There are seventeen tours,
                  covering products, orders, the site editor, SEO, domains and
                  inventory.
                </p>
                <div>
                  <p data-motion="copy">
                    The analytics tour is ten steps. Step four, in the capture,
                    explains the visits chart: the dark line is total page
                    views, the light one is unique visitors per day. Because
                    there are no cookies, unique visitors reset at midnight UTC,
                    and someone who comes back every day for a week counts seven
                    times.
                  </p>
                  <p data-motion="copy">
                    That is the price of counting without cookies, and the tour
                    says so in plain words where the seller will read it. A
                    seller who has never run a shop gets through the first week
                    without a support ticket.
                  </p>
                </div>
              </div>
            </section>

            <figure class="case-screen" data-motion-group>
              <a
                class="screen-window"
                href={`${assets}/analytics-tour.webp`}
                target="_blank"
                rel="noreferrer"
                aria-label="Open the Sellphy analytics tour screenshot at full size"
                data-motion="cel"
              >
                <div class="screen-bar" aria-hidden="true">
                  <span></span><span></span><span></span><b
                    >Analytics / tour step 4 of 10</b
                  >
                </div>
                <img
                  src={`${assets}/analytics-tour.webp`}
                  alt="Sellphy analytics page with a guided tour popup on step 4 of 10, explaining the visits chart"
                  width="1024"
                  height="662"
                  loading="lazy"
                />
              </a>
              <figcaption>
                <strong>Step 4 of 10</strong><span
                  >The tour names both lines on the chart before it moves on to
                  the tables below. Demo shop; select to enlarge.</span
                >
              </figcaption>
            </figure>

            <section class="case-section" id="ux" data-motion-group>
              <h2 data-motion="title">Nobody has to learn the menu.</h2>
              <div class="case-columns">
                <p class="case-lede" data-motion="copy">
                  One shortcut opens search from any dashboard screen. A seller
                  types what they are after and gets the exact place it lives:
                  the order mode toggle inside Shop features, the orders and
                  revenue block inside Analytics, the order list itself. Enter
                  opens it. Nobody has to remember which menu a setting is
                  under.
                </p>
                <div>
                  <p data-motion="copy">
                    Bulgarian sellers often have the keyboard in Latin mode, so
                    the search reads both alphabets. In the capture the seller
                    typed porachki and the results are for Поръчки, orders, with
                    the matched letters marked. Each hit lists its sub-sections,
                    so the seller lands on the part of the page they wanted
                    rather than the top of it.
                  </p>
                  <p data-motion="copy">
                    I made the same trade everywhere in the dashboard: fewer
                    clicks and less to remember. A tour explains a screen once,
                    and after that search means nobody has to keep a map of the
                    menu in their head. The site editor lets you type into the
                    page instead of finding the field for it. The products table
                    lets you tick rows and edit them together. A seller with a
                    shop to run notices those things before they notice a
                    feature list.
                  </p>
                </div>
              </div>
            </section>

            <figure class="case-screen sellphy-search-screen" data-motion-group>
              <a
                class="screen-window"
                href={`${assets}/command-search.webp`}
                target="_blank"
                rel="noreferrer"
                aria-label="Open the Sellphy dashboard search screenshot at full size"
                data-motion="cel"
              >
                <div class="screen-bar" aria-hidden="true">
                  <span></span><span></span><span></span><b
                    >Dashboard / search</b
                  >
                </div>
                <img
                  src={`${assets}/command-search.webp`}
                  alt="Sellphy dashboard search with porachki typed in Latin letters, showing Bulgarian results for orders grouped under Orders, Shop features and Analytics with matched letters highlighted"
                  width="922"
                  height="661"
                  loading="lazy"
                />
              </a>
              <figcaption>
                <strong>Latin in, Cyrillic out</strong><span
                  >porachki, typed in Latin letters, matched against the
                  Bulgarian labels for orders. Each hit lists its sub-sections,
                  and Enter opens the top one. Demo shop; select to enlarge.</span
                >
              </figcaption>
            </figure>

            <section class="case-section" id="static" data-motion-group>
              <h2 data-motion="title">Fast pages, small bill.</h2>
              <div class="case-columns">
                <p class="case-lede" data-motion="copy">
                  A customer opening a Sellphy shop gets a page that was built
                  once and is served as a file. It loads in the time it takes to
                  send it, it does not wait on a database, and a traffic spike
                  from a promotion is the same file going out more times.
                </p>
                <div>
                  <p data-motion="copy">
                    The home, shop, category, about, services and legal pages
                    work this way. Cart, checkout and the dashboard stay
                    dynamic. When a seller saves a change, only the affected
                    paths are rebuilt, so an edit shows up in seconds without
                    touching the rest of the shop. Category filters would
                    normally defeat the cache, so a proxy folds the filter into
                    an internal path while the public URL stays readable.
                  </p>
                  <p data-motion="copy">
                    Once the storefronts were files I no longer needed Vercel.
                    The whole platform runs in Docker on a Hetzner VPS behind
                    Caddy, and the hosting bill is 83% lower than before. Moving
                    all of my own shops onto it saves me 25 or more hours of
                    maintenance a month, which is time that now goes into the
                    product.
                  </p>
                </div>
              </div>
            </section>

            <section class="case-next" data-motion-group>
              <div>
                <h2 data-motion="title">
                  Next up:<br /><span class="title-outline">Finance Me.</span>
                </h2>
                <p data-motion="copy">
                  A family expense tracker that picks up Apple Pay purchases
                  through an iOS Shortcut.
                </p>
              </div>
              <a
                class="button button-light"
                href="/work/finance-me"
                data-squash
                data-motion="action">View Finance Me <ArrowIcon /></a
              >
            </section>
          </div>
        </div>
      </article>
    </main>
  </div>
</BaseLayout>

<style>
  .sellphy-hero {
    grid-template-columns: minmax(19rem, 0.72fr) minmax(0, 1.28fr);
  }

  .sellphy-stage {
    min-width: 0;
  }
  .sellphy-stage figcaption {
    margin-top: 1rem;
    font-size: 1rem;
    line-height: 1.4;
  }
  .sellphy-stage figcaption span {
    display: block;
    color: #55534c;
  }
  .sellphy-case .screen-window > img {
    aspect-ratio: auto;
    height: auto;
    filter: none;
  }
  .sellphy-case .screen-window {
    border-color: #77736a;
  }
  .sellphy-stage .screen-window {
    border-color: var(--ink);
  }

  .sellphy-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    margin: 0 0 clamp(3.5rem, 7vw, 7rem);
    border-block: 2px solid var(--paper-light);
  }
  .sellphy-stats div {
    padding: 1.5rem 1rem;
  }
  .sellphy-stats div + div {
    border-left: 1px solid #77736a;
  }
  .sellphy-stats dt {
    font: 700 clamp(2.4rem, 3.6vw, 3.4rem) / 1 var(--font-display);
    letter-spacing: -0.02em;
    color: var(--paper-light);
  }
  .sellphy-stats dd {
    margin: 0.7rem 0 0;
    max-width: 24ch;
    color: var(--smoke-light);
    line-height: 1.4;
  }

  .setup-sequence {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    margin-top: 3rem;
    padding: 0;
    list-style: none;
    border-block: 2px solid var(--paper-light);
  }

  .setup-sequence li {
    padding: 1.5rem 1rem;
  }
  .setup-sequence li + li {
    border-left: 1px solid #77736a;
  }
  .setup-sequence li:nth-child(4) {
    border-left: 0;
  }
  .setup-sequence li:nth-child(n + 4) {
    border-top: 1px solid #77736a;
  }
  .setup-sequence strong {
    display: block;
    font: 600 1.1rem var(--font-display);
  }
  .setup-sequence span {
    display: block;
    margin-top: 0.4rem;
    color: var(--smoke-light);
  }
  .sellphy-product-strip {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
    padding: clamp(2rem, 5vw, 4rem) 0 clamp(4rem, 8vw, 8rem);
    border-bottom: 1px solid #4a4943;
  }
  .sellphy-product-strip figcaption {
    display: grid;
    gap: 0.35rem;
    padding: 0.9rem 0.15rem 0;
    color: var(--smoke-light);
  }
  .sellphy-product-strip figcaption strong {
    color: var(--paper-light);
    font-family: var(--font-display);
    font-size: 1.05rem;
  }

  .sellphy-search-screen .screen-window {
    max-width: 57.75rem;
  }

  .sellphy-stacked-pair {
    grid-template-columns: 1fr;
    gap: 3rem;
  }
  .sellphy-stacked-pair figure:nth-child(2) {
    padding-top: 0;
  }
  .sellphy-stacked-pair figcaption {
    grid-template-columns: 0.35fr 0.65fr;
    gap: 2rem;
  }

  @media (max-width: 1100px) {
    .sellphy-hero {
      grid-template-columns: 1fr;
    }
    .sellphy-hero .case-meta {
      margin-top: 0;
    }
    .sellphy-stage {
      max-width: 58rem;
    }
    .sellphy-stats {
      grid-template-columns: 1fr 1fr;
    }
    .sellphy-stats div:nth-child(3) {
      border-left: 0;
    }
    .sellphy-stats div:nth-child(n + 3) {
      border-top: 1px solid #77736a;
    }
    .sellphy-product-strip {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
  }

  @media (max-width: 700px) {
    .sellphy-stacked-pair figcaption {
      grid-template-columns: 1fr;
      gap: 0.45rem;
    }
    .sellphy-stats {
      grid-template-columns: 1fr;
    }
    .sellphy-stats div {
      padding-inline: 0;
    }
    .sellphy-stats div + div {
      border-left: 0;
      border-top: 1px solid #77736a;
    }
    .setup-sequence {
      grid-template-columns: 1fr;
    }
    .setup-sequence li {
      padding-inline: 0;
    }
    .setup-sequence li + li,
    .setup-sequence li:nth-child(4) {
      border-left: 0;
    }
    .setup-sequence li + li,
    .setup-sequence li:nth-child(n + 4) {
      border-top: 1px solid #77736a;
    }
    .sellphy-stage figcaption {
      font-size: 0.95rem;
    }
  }
</style>
